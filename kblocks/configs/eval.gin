# Training config template for basic supervised learning
# may set `is_meta_model = True` for models using `kblocks.meta_models`.

# REQUIRES:
# problem
# pipeline_fn
# optimizer_fn
# epochs or total_train_steps
# model_dir

import kblocks.framework.steps
import kblocks.cli
include 'utils/trainable.gin'

kb.main.fn = @kb.framework.evaluate
kb.framework.evaluate.trainable = %trainable  # default defined in trainable.gin

kb.framework.evaluate.batch_size = %batch_size
