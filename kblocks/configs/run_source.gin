# Requires macro:
#  - source
#  - callback

import kblocks.framework.source

kb.main.fn = @kb.framework.run_source
kb.framework.run_source.source = %source
kb.framework.run_source.split = %split
kb.framework.run_source.callback = %callback

shuffle = True
split = 'train'
callback = None
